let nome, nota1, nota2


function clique(){
    nome = prompt("Digite seu nome")
    nota1 = parseFloat(prompt("primeira nota";))
    nota2 = parseFloat(prompt("Segunda nota";))

    media = (nota1 + nota2) / 2
    if(media <= 5){
     
        let ()
    }
    document.write(`A media de ${nome} foi ${media}`)
    document.write(if {media =>})
}